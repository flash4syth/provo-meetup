defmodule NumberSumming do

  # These sum function definitions `def sum(argument)` will be checked from top to bottom
  def sum(_replace_with_integer) do
    # put your code here
  end
  # We're using a guard clause here `when n > 0` to disallow negative integers
  def sum(n) when n > 0 do
    # put your code here
  end
  # advanced function when the remainder is equal to 0 (e.g. remainder of an
  # integer x divided by y `rem(x,y) => 0`
  # def sum(n) when rem(n, y) == 0
    # your code here, also replace y above with an integer
  # end
end
